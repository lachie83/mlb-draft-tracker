# Dedicated, minimal ServiceAccount. The app never talks to the Kubernetes
# API, so its own token is never mounted into the pod.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: mlb-draft-tracker
  namespace: mlb-draft-tracker
automountServiceAccountToken: false
